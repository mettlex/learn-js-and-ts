// Procedural Programming

const todoList = [];

const todo1 = {
  text: "Have water",
  done: true,
};

const todo2 = {
  text: "Have dinner",
  done: false,
  andaje: "ki sob faltu",
};

todoList.push(todo1);
todoList.push(todo2);

console.log(todoList.length);
console.log(todoList);
